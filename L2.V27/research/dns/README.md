The DNS experiment is implemented by `../../scripts/run-dns-experiment.sh`.
It creates a disposable network, starts dnsmasq, then compares a glibc-based
Ubuntu container with a musl-based Alpine container. Save the command output to
`L2.V27/results/dns.log` and paste the factual result into the report.
