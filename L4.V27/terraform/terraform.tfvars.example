# Copy to terraform.tfvars only if your local environment differs from defaults.
# terraform.tfvars is ignored because it can contain machine-specific paths.
ssh_public_key_path = "~/.ssh/id_ed25519.pub"
# storage_pool = "default"
# libvirt_uri = "qemu:///system"
