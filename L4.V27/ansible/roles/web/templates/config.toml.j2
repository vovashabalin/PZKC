[app]
host = "127.0.0.1"
port = {{ app_port }}

[database]
host = "{{ hostvars['db'].ansible_host }}"
port = {{ db_port }}
name = "{{ db_name }}"
user = "{{ db_user }}"
password = {{ db_password | to_json }}
